<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="traumConfigDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="windowFixedWidthMajor">70%</item>
        <item name="windowFixedWidthMinor">70%</item>
        <item name="windowFixedHeightMajor">90%</item>
        <item name="windowFixedHeightMinor">70%</item>
    </style>

    <style name="traumConfigDialogRow" parent="Theme.AppCompat.Light">
        <item name="android:paddingBottom">15dp</item>

    </style>


</resources>